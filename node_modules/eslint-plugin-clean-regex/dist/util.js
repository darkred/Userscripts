"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findLastIndex = exports.findIndex = exports.assertNever = void 0;
function assertNever(value) {
    throw new Error(`This part of the code should never be reached but ${value} made it through.`);
}
exports.assertNever = assertNever;
function findIndex(arr, condFn) {
    return arr.findIndex(condFn);
}
exports.findIndex = findIndex;
function findLastIndex(arr, condFn) {
    for (let i = arr.length - 1; i >= 0; i--) {
        if (condFn(arr[i], i)) {
            return i;
        }
    }
    return -1;
}
exports.findLastIndex = findLastIndex;
